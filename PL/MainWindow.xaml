<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="400"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="25" />
            <Setter Property="FontFamily" Value="Comic Sans MS" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="BorderBrush" Value="BlacK"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="133*"/>
            <RowDefinition Height="47*"/>
            <RowDefinition Height="19*"/>
        </Grid.RowDefinitions>
        <Label Content="Enter Your ID:" HorizontalAlignment="Center" Grid.Row="1" VerticalAlignment="Top" Margin="0,33,0,0" Height="33" Width="192" FontFamily="Cooper Black" FontSize="24"/>
        <TextBox Grid.Row="1" Margin="0,82,0,22" BorderBrush="#FF090909" HorizontalAlignment="Center" Width="324"
             Text="{Binding TextInput, Mode=TwoWay}" TextChanged="TextBox_TextChanged" KeyDown="TextBox_KeyDown"/>
        <Button Content="Enter" Height="37" Margin="130,0,0,0" Grid.Row="2" Width="116" Click="btn_enter"/>
    </Grid>
</Window>
<!--, UpdateSourceTrigger=PropertyChanged-->




    
