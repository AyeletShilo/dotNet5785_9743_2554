<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="25" />
            <Setter Property="FontFamily" Value="Comic Sans MS" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="BorderBrush" Value="BlacK"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="105*"/>
            <RowDefinition Height="137*"/>
            <RowDefinition Height="61*"/>
            <RowDefinition Height="110*"/>
            <RowDefinition Height="22*"/>
        </Grid.RowDefinitions>
        <Label Content="Enter Your ID:" HorizontalAlignment="Center" Grid.Row="1" VerticalAlignment="Top" Margin="0,15,0,0" Height="33" Width="192" FontFamily="Cooper Black" FontSize="24"/>
        <TextBox Grid.Row="1" Margin="0,61,0,28" BorderBrush="#FF090909" HorizontalAlignment="Center" Width="324"
             Text="{Binding TextInput, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>
        <Button Content="Volunteer window" Height="90" Margin="74,0,0,0" Grid.Row="3" Width="232"
                IsEnabled="{Binding IsButtonVEnabled}"/>
        <Button Content="Main window" Height="90" Margin="498,0,0,0" Grid.Row="3" Width="232"
                IsEnabled="{Binding IsButtonMEnabled}" Click="btn_OpenWindow"/>
    </Grid>
</Window>




    
