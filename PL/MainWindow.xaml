<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Icon="/BigIcon.png">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="25" />
            <Setter Property="FontFamily" Value="Comic Sans MS" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="BorderBrush" Value="BlacK"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <!--Login Grid-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="120*"/>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Row="1" BorderBrush="#FF090909" HorizontalAlignment="Stretch" 
            Text="{Binding Id, Mode=TwoWay}" TextChanged="ID_TextChanged" KeyDown="ID_KeyDown" Grid.Column="1"/>

        <!--#region -->
        <!--<Label Content="Enter Your ID:" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="92,0,0,0" Height="33" Width="192" FontFamily="Cooper Black" FontSize="24"/>-->
        <!--<StackPanel Margin="0,0,0,45" Grid.RowSpan="3">-->
        <!--KeyDown="TextBox_KeyDown"-->
        <!---->
        <!--<TextBox Grid.Row="1" Margin="0,82,0,22" BorderBrush="#FF090909" HorizontalAlignment="Center" Width="324"
            Text="{Binding PassInput, Mode=TwoWay}" TextChanged="Pass_TextChanged"/>-->
        <!--KeyDown="TextBox_KeyDown"-->
        <!--</StackPanel>-->
        <!--#endregion-->
        <PasswordBox 
             Grid.Row="3" Grid.Column="1" BorderBrush="#FF090909" HorizontalAlignment="Stretch" 
             PasswordChar="*" PasswordChanged="PasswordBox_PasswordChanged" KeyDown="TextBox_KeyDown"/>
        
        <Button Content="Login"  HorizontalAlignment="Center"  Grid.Row="4" Grid.Column="1" Click="btn_Login" Width="151"/>
    </Grid>

</Window>
<!--, UpdateSourceTrigger=PropertyChanged-->




    
